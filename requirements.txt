# City Center Voice Service (Chatterbox)
# Python >= 3.10 required by chatterbox-tts.

chatterbox-tts>=0.1.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
PyJWT>=2.9.0
httpx>=0.27.0

# torch / torchaudio are pulled in by chatterbox-tts, but pin CUDA builds via
# the Docker base image (see Dockerfile) or install from the PyTorch index for
# your CUDA version, e.g.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
